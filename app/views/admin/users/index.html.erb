<h1>ユーザー一覧</h1>

<%= link_to "新規登録", new_admin_user_path, class: "btn btn-danger" %>

<table class="table table-striped">
  <tr>
    <th>name</th>
    <th>email</th>
    <th>admin</th>
	</tr>

  <% @users.each do |user| %>
    <td><%= user.user_name %></td>
    <td><%= user.email %></td>
    <%# <% if user.admin == true %>
    <%# <td><%= あり %></td> %>
    <%# <% else %> %>
    <%# <td><%= なし %></td> %> %>
    <%# <% end %> %>
    <td><%= link_to '詳細を確認する', admin_user_path(user.id), class: "btn btn-outline-primary" %></td>
    <td><%= link_to 'タスクを編集する', edit_admin_user_path(user.id), class: "btn btn-outline-secondary" %></td>
    <td><%= link_to 'タスクを削除する', admin_user_path(user.id), method: :delete ,data: { confirm: "本当に削除してもいいですか?" }, class: "btn btn-outline-success" %></td>
	</tr>
<% end %>